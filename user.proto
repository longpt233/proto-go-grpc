syntax = "proto3";

package user;

option go_package= "pb";  // tên pack được proto gen ra

message String {
    string name = 1;
    int32 id = 2;
    int32 age =3;
}

service HelloService {
    rpc Hello (String) returns (String);
}